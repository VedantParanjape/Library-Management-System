#include<iostream>
using namespace std;

int validate(int acid);

int validate(int acid)
{
	int flag = 1;
	
	for(int iou=0;iou<user_total;iou++)
	{
		if(usr_bookid[iou] = acid && usr_bookid != 0)
		{
			flag = 0;
		}
	}
	return flag;
}

